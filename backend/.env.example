GROQ_API_KEY=your_groq_api_key_here

# Comma-separated list of extra allowed CORS origins (e.g. your deployed frontend).
# Localhost is always allowed automatically for local dev - this is only needed in production.
# Example: ALLOWED_ORIGINS=https://ai-interview-coach.vercel.app
ALLOWED_ORIGINS=
